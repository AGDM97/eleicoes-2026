# Configuração opcional para pytest

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers

markers =
    slow: marca testes como lentos (deselect with '-m "not slow"')
    integration: marca testes de integração
